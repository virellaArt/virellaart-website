---
import BaseLayout from "./BaseLayout.astro";
import {
  getLanguageFromPath,
  localizedPath,
} from "../i18n/config";

interface FAQItem {
  question: string;
  answer: string;
}

interface AlternateURL {
  language: string;
  href: string;
}

interface Props {
  title: string;
  description: string;
  image: string;
  imageAlt: string;
  publishedTime: string;
  modifiedTime?: string;
  section?: string;
  keywords?: string[];
  authorName?: string;
  robots?: string;
  readingTimeMinutes?: number;
  wordCount?: number;
  faqItems?: FAQItem[];
  alternateURLs?: AlternateURL[];
  xDefaultURL?: string;
  includeCanonical?: boolean;
  switcherFallbackToLocalizedPath?: boolean;
}

const {
  title,
  description,
  image,
  imageAlt,
  publishedTime,
  modifiedTime = publishedTime,
  section,
  keywords = [],
  authorName = "VIRELLAART",
  robots = "index, follow",
  readingTimeMinutes,
  wordCount,
  faqItems = [],
  alternateURLs,
  xDefaultURL,
  includeCanonical = true,
  switcherFallbackToLocalizedPath = false,
} = Astro.props;

const siteURL = "https://www.virellaart.com";
const language = getLanguageFromPath(Astro.url.pathname);

const canonicalPath =
  Astro.url.pathname.replace(/\/?$/, "/");

const canonicalURL = new URL(
  canonicalPath,
  siteURL,
).href;

const imageURL = new URL(
  image,
  siteURL,
).href;

const articleIntent = `${title} ${section ?? ""} ${keywords.join(" ")}`.toLowerCase();

const commercialPath = articleIntent.includes("bedroom") || articleIntent.includes(" bed ")
  ? "/bedrooms"
  : articleIntent.includes("dining")
    ? "/dining-rooms"
    : articleIntent.includes("tv unit") || articleIntent.includes("media furniture")
      ? "/tv-units"
      : articleIntent.includes("sofa") || articleIntent.includes("living room")
        ? "/living-rooms"
        : "/collections/luxury-classic";

const commercialLabel = commercialPath === "/bedrooms"
  ? "Explore Bedroom Collections"
  : commercialPath === "/dining-rooms"
    ? "Explore Dining Room Collections"
    : commercialPath === "/tv-units"
      ? "Explore TV Unit Collections"
      : commercialPath === "/living-rooms"
        ? "Explore Living Room Collections"
        : "Explore VIRELLAART Collections";

const commercialURL = localizedPath(commercialPath, language);
const quotationMessage = [
  "Hello VIRELLAART, I am reading this buying guide:",
  title,
  `Page: ${canonicalURL}`,
  "I would like a product and delivery quotation.",
  "Preferred model or collection:",
  "Delivery country:",
  "Delivery city:",
].join("\n");
const quotationURL = `https://wa.me/905539478608?text=${encodeURIComponent(quotationMessage)}`;

const articleSchema = {
  "@context": "https://schema.org",
  "@type": "Article",
  "@id": `${canonicalURL}#article`,
  headline: title,
  description,
  image: [imageURL],
  datePublished: publishedTime,
  dateModified: modifiedTime,
  inLanguage: language,
  isAccessibleForFree: true,
  articleSection: section,
  keywords:
    keywords.length > 0
      ? keywords.join(", ")
      : undefined,
  wordCount,
  timeRequired:
    readingTimeMinutes
      ? `PT${readingTimeMinutes}M`
      : undefined,
  author: {
    "@type": "Organization",
    "@id": `${siteURL}/#organization`,
    name: authorName,
    url: siteURL,
  },
  publisher: {
    "@type": "Organization",
    "@id": `${siteURL}/#organization`,
    name: "VIRELLAART",
    logo: {
      "@type": "ImageObject",
      url: `${siteURL}/favicon.svg`,
    },
  },
  mainEntityOfPage: {
    "@type": "WebPage",
    "@id": canonicalURL,
  },
  isPartOf: {
    "@type": "WebSite",
    "@id": `${siteURL}/#website`,
  },
};

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "@id": `${canonicalURL}#breadcrumb`,
  itemListElement: [
    {
      "@type": "ListItem",
      position: 1,
      name: "Home",
      item: new URL(
        localizedPath("/", language),
        siteURL,
      ).href,
    },
    {
      "@type": "ListItem",
      position: 2,
      name: "Journal",
      item: new URL(
        localizedPath("/blog", language),
        siteURL,
      ).href,
    },
    {
      "@type": "ListItem",
      position: 3,
      name: title,
      item: canonicalURL,
    },
  ],
};

const faqSchema =
  faqItems.length > 0
    ? {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "@id": `${canonicalURL}#faq`,
        mainEntity: faqItems.map((item) => ({
          "@type": "Question",
          name: item.question,
          acceptedAnswer: {
            "@type": "Answer",
            text: item.answer,
          },
        })),
      }
    : undefined;
---

<BaseLayout
  {title}
  {description}
  {image}
  {imageAlt}
  {robots}
  {alternateURLs}
  {switcherFallbackToLocalizedPath}
  {xDefaultURL}
  {includeCanonical}
  pageType="article"
>
  <Fragment slot="head">
    <meta
      name="author"
      content={authorName}
    />

    <meta
      property="article:published_time"
      content={publishedTime}
    />

    <meta
      property="article:modified_time"
      content={modifiedTime}
    />

    {
      section && (
        <meta
          property="article:section"
          content={section}
        />
      )
    }

    {
      keywords.map((keyword) => (
        <meta
          property="article:tag"
          content={keyword}
        />
      ))
    }

    <script
      type="application/ld+json"
      set:html={JSON.stringify(articleSchema)}
    />

    <script
      type="application/ld+json"
      set:html={JSON.stringify(breadcrumbSchema)}
    />

    {
      faqSchema && (
        <script
          type="application/ld+json"
          set:html={JSON.stringify(faqSchema)}
        />
      )
    }
  </Fragment>

  <slot />

  <section class="article-commercial-bridge" aria-labelledby="article-commercial-title">
    <div class="article-commercial-shell">
      <p class="article-commercial-eyebrow">From guide to quotation</p>
      <h2 id="article-commercial-title">Ready to compare a real VIRELLAART model?</h2>
      <p>
        Continue with the relevant collection, then send the model together with your delivery country and city. Your WhatsApp request will include this guide so the conversation keeps its context.
      </p>
      <div class="article-commercial-actions">
        <a class="article-commercial-secondary" href={commercialURL}>
          {commercialLabel}
        </a>
        <a
          class="article-commercial-primary"
          href={quotationURL}
          target="_blank"
          rel="noopener noreferrer"
          data-whatsapp-context="article-commercial-bridge"
        >
          Get Price & Delivery Quote
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .article-commercial-bridge {
    padding: 56px 20px 72px;
    background: #17120f;
    color: #f7f3ed;
  }

  .article-commercial-shell {
    width: min(900px, 100%);
    margin: 0 auto;
    text-align: center;
  }

  .article-commercial-eyebrow {
    margin: 0 0 10px;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    opacity: 0.72;
  }

  .article-commercial-shell h2 {
    margin: 0;
    font-size: clamp(1.8rem, 4vw, 3rem);
    line-height: 1.08;
  }

  .article-commercial-shell > p:not(.article-commercial-eyebrow) {
    max-width: 700px;
    margin: 18px auto 0;
    line-height: 1.7;
    opacity: 0.84;
  }

  .article-commercial-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 12px;
    margin-top: 28px;
  }

  .article-commercial-actions a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 48px;
    padding: 12px 20px;
    border-radius: 999px;
    font-weight: 700;
    text-decoration: none;
  }

  .article-commercial-primary {
    background: #f7f3ed;
    color: #17120f;
  }

  .article-commercial-secondary {
    border: 1px solid rgba(247, 243, 237, 0.35);
    color: #f7f3ed;
  }

  @media (max-width: 640px) {
    .article-commercial-bridge {
      padding: 44px 16px 56px;
    }

    .article-commercial-actions {
      flex-direction: column;
    }

    .article-commercial-actions a {
      width: 100%;
    }
  }
</style>
